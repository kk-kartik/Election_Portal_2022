import React from "react";
import { Route, Routes } from "react-router-dom";

const NominationScreen = () => {
  return (
    <>
      {/* Nominations steps list component - add here */}
      {/* sidenav and gallery */}
      <Routes>{/* setup react router for the sidenav */}</Routes>
      {/* gallery as per the active routes */}
    </>
  );
};
export default NominationScreen;
